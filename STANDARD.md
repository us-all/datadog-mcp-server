# Moved

The `@us-all` MCP server standard now lives at:

**https://github.com/us-all/mcp-toolkit/blob/main/STANDARD.md**

The patterns it describes are also available as a pre-built npm package:

```bash
pnpm add @us-all/mcp-toolkit
```

This file is kept as a redirect for old links.
